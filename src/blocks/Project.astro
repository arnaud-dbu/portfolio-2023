---
import P from "../components/P.astro";
export interface Props {
	title: string;
	categories: string;
	year: string;
	href: string;
}

const { title, categories, year, href } = Astro.props;
---

<a href={href} class="project bottom-line relative flex flex-col justify-between py-14 xl:py-20">
	<div class="flex items-center justify-between">
		<span class="font-serif text-5xl uppercase xl:text-8xl">{title}</span>
		<span class="text-2xl opacity-20 xl:text-5xl">/{year}</span>
	</div>
	<P size="sm">{categories}</P>
</a>
