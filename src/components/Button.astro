---
export interface Props {
	label: string;
	href: string;
}

const { label, href } = Astro.props;
---

<a
	href={href}
	class="rounded-lg bg-black px-8 py-4 font-sans text-sm font-light uppercase tracking-widest text-white"
	>{label}</a
>
