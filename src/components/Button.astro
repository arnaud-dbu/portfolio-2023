---
export interface Props {
	label: string;
	href: string;
}

const { label, href } = Astro.props;
---

<a
	href={href}
	class="text-md border-[1.5px] border-black bg-black p-4 px-10 font-sans font-light uppercase tracking-wide text-white"
	>{label}</a
>
