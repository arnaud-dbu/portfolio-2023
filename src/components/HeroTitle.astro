---
export interface Props {
	title: string;
	className?: string;
	animation?: string;
}

const { title, animation, className } = Astro.props;
---

<span
	class=`${animation} mask-container font-serif text-[12vw]  uppercase leading-none sm:text-[10vw] lg:text-[9vw] 2xl:text-10xl`
	>{title}</span
>
